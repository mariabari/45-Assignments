// maria bari

// cars

function cars_info(manufacturer: string, model: number){
    let cars = {
        manufacturer,
        model,
    }
return cars
}
let car1 = cars_info("Honda civic", 2020);
console.log(car1)

function carsInfo(manufacturer: string, model: number) {
    let car = {
      manufacturer,
      model,
    };
    return car;
  }